{
  "description": "Nova Engineering — 자동 적용 규칙 주입 + NOVA-STATE.md 자동 생성",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\""
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/init-nova-state.sh\""
          }
        ]
      }
    ]
  }
}
