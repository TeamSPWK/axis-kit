# Credentials
.env
.env.*
!.env.example
.secret/
*.pem
*accessKeys*

# OS
.DS_Store
Thumbs.db

# Project outputs (사용자별 산출물)
docs/plans/
docs/designs/
docs/verifications/

# 세션 상태 파일 (사용자별, 커밋 금지)
NOVA-STATE.md
.nova-orchestration.json

# record-event.sh CI 분기 출력 (CI=true + CI_ARTIFACTS 미지정 시 프로젝트 루트에 생성됨)
nova-events/

# bkit generated files
docs/.bkit-memory.json
docs/.pdca-status.json

# Claude Code local settings (per-user)
.claude/settings.local.json

# IDE
.vscode/
.idea/

# Dependencies
node_modules/

# MCP server
mcp-server/node_modules/
mcp-server/dist/

# Nova Orbit (별도 프로젝트)
nova-orbit/
.nova/
